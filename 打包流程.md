# 悬浮提词器 - TestFlight 打包流程

> 记录日期：2026-05-13
> 适用版本：v1.0.3 及以上

---

## 前置条件

1. Xcode 已安装并登录 Apple ID
2. 项目配置：
   - Bundle ID: `com.teleprompter.1777642669`
   - Development Team: `2P5UG67845` (vince guo)
   - 签名方式：Automatic
3. 证书要求：
   - Apple Distribution 证书（用于 TestFlight/上架）
   - 或 Apple Development 证书（用于开发调试）

---

## 快速打包（一键命令）

```bash
cd /Users/ghsh/.qclaw/workspace/Teleprompter

# 1. Clean + Archive
xcodebuild -project Teleprompter.xcodeproj \
  -scheme Teleprompter \
  -sdk iphoneos \
  -configuration Release \
  CODE_SIGN_STYLE=Automatic \
  DEVELOPMENT_TEAM=2P5UG67845 \
  -archivePath build/Teleprompter.xcarchive \
  archive

# 2. 导出 TestFlight IPA
cat > build/ExportOptions.plist << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store-connect</string>
    <key>teamID</key>
    <string>2P5UG67845</string>
    <key>signingStyle</key>
    <string>automatic</string>
    <key>stripSwiftSymbols</key>
    <true/>
    <key>uploadSymbols</key>
    <false/>
</dict>
</plist>
EOF

xcodebuild -exportArchive \
  -archivePath build/Teleprompter.xcarchive \
  -exportPath build/export \
  -exportOptionsPlist build/ExportOptions.plist \
  -allowProvisioningUpdates

# 3. 复制到桌面
cp build/export/Teleprompter.ipa \
  ~/Desktop/悬浮提词器安装包/Teleprompter_TestFlight_v$(date +%Y%m%d_%H%M).ipa
```

---

## 分步详解

### 第一步：Archive（归档）

```bash
xcodebuild -project Teleprompter.xcodeproj \
  -scheme Teleprompter \
  -sdk iphoneos \
  -configuration Release \
  CODE_SIGN_STYLE=Automatic \
  DEVELOPMENT_TEAM=2P5UG67845 \
  -archivePath build/Teleprompter.xcarchive \
  archive
```

**关键参数**：
- `-sdk iphoneos`：真机版本（不要用 iphonesimulator）
- `CODE_SIGN_STYLE=Automatic`：自动签名
- `DEVELOPMENT_TEAM=2P5UG67845`：指定 Team ID
- `-allowProvisioningUpdates`：自动下载/更新证书

**常见问题**：
- `No Account for Team` → 检查 Xcode 是否登录了正确的 Apple ID
- `No profiles found` → 添加 `-allowProvisioningUpdates` 参数

---

### 第二步：导出 IPA

创建 `ExportOptions.plist` 文件：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- 导出方式：app-store-connect = TestFlight / App Store -->
    <key>method</key>
    <string>app-store-connect</string>
    
    <!-- Team ID -->
    <key>teamID</key>
    <string>2P5UG67845</string>
    
    <!-- 签名方式：automatic = 自动管理证书 -->
    <key>signingStyle</key>
    <string>automatic</string>
    
    <!-- 去除 Swift 符号（减小包体积） -->
    <key>stripSwiftSymbols</key>
    <true/>
    
    <!-- 不上传符号表（后续可单独上传 dSYM） -->
    <key>uploadSymbols</key>
    <false/>
</dict>
</plist>
```

执行导出：

```bash
xcodebuild -exportArchive \
  -archivePath build/Teleprompter.xcarchive \
  -exportPath build/export \
  -exportOptionsPlist build/ExportOptions.plist \
  -allowProvisioningUpdates
```

---

## 导出方式对比

| 方式 | method 值 | 用途 | 证书要求 |
|------|-----------|------|----------|
| **TestFlight** | `app-store-connect` | 上架 / 测试 | Apple Distribution |
| **Ad Hoc** | `ad-hoc` | 内部分发 | Apple Distribution + 设备 UDID |
| **Development** | `development` | 开发调试 | Apple Development |
| **Enterprise** | `enterprise` | 企业分发 | Apple Enterprise |

---

## 证书信息

```bash
# 查看可用证书
security find-identity -v -p codesigning
```

当前可用证书：
1. `Apple Development: ghshcln@126.com (YHA33SKZ65)` — 开发调试
2. `Apple Distribution: vince guo (2P5UG67845)` — TestFlight/上架

---

## 上传 TestFlight

### 方式一：Xcode 上传（推荐）

1. Xcode → Window → Organizer
2. 选择 Archive → Distribute App
3. App Store Connect → Upload

### 方式二：Transporter App

1. 下载 [Transporter](https://apps.apple.com/us/app/transporter/id1450874784)
2. 拖拽 IPA 文件上传

### 方式三：命令行（altool）

```bash
xcrun altool --upload-app \
  -f ~/Desktop/悬浮提词器安装包/Teleprompter_TestFlight_v1.0.3.ipa \
  -t ios \
  -u "你的AppleID邮箱" \
  -p "专用密码"
```

---

## 文件输出

| 文件 | 路径 |
|------|------|
| Archive | `build/Teleprompter.xcarchive` |
| IPA | `build/export/Teleprompter.ipa` |
| 最终文件 | `~/Desktop/悬浮提词器安装包/Teleprompter_TestFlight_v*.ipa` |

---

## 注意事项

1. **Team ID**：项目配置里是 `2P5UG67845`（vince guo），不是 `YHA33SKZ65`
2. **自动签名**：使用 `CODE_SIGN_STYLE=Automatic` + `-allowProvisioningUpdates`
3. **Distribution 证书**：TestFlight 必须用 Apple Distribution 证书
4. **版本号**：上传前检查 `Info.plist` 中的 `CFBundleShortVersionString` 和 `CFBundleVersion`

## Build 版本更新规范（重要）

**每次打包前必须更新 Build 号**，否则会上传失败（错误：捆绑包版本必须高于之前上传的版本）。

### 更新步骤

1. **检查当前 Build 号**：
```bash
grep -A1 'CFBundleVersion' Teleprompter/Info.plist
```

2. **更新 Build 号**（递增 +1）：
```bash
# 例如从 25 更新到 26
sed -i '' 's/<string>25<\/string>/<string>26<\/string>/g' Teleprompter/Info.plist
```

3. **验证更新成功**：
```bash
grep -A1 'CFBundleVersion' Teleprompter/Info.plist
```

### Build 号历史记录

| Build | 版本 | 日期 | 说明 |
|-------|------|------|------|
| 24 | v1.0.3 | 2026-05-13 | 重写会员购买页 |
| 25 | v1.0.9 | 2026-05-14 | 修复打包路径问题 |
| 26 | v1.0.9 | 2026-05-14 | 会员有效期显示优化、免费次数累加展示 |

---

## 历史版本

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.3 | 2026-05-13 | 重写会员购买页，修复重复购买 Bug |
| v1.0.2 | 2026-05-12 | 修复内购价格显示 |
| v1.0.1 | 2026-05-10 | 初始 TestFlight 版本 |
