# 悬浮提词器 - 会员购买页产品设计要求

> 版本：v1.1 | 更新日期：2026-05-13
> 状态：已确认生效，代码不做更改
> 变更记录：v1.1 "按次付费"统一更名为"次卡"

## 核心分类（代码必须遵守）

| 产品 | 类型 | StoreKit 类型 | 说明 |
|------|------|--------------|------|
| 次卡 | **消费型** | Consumable | 不是订阅，用完即止，可累加次数 |
| 月卡 | **订阅型** | Auto-Renewable Subscription | 自动续费，App Store 管理 |
| 年卡 | **订阅型** | Auto-Renewable Subscription | 自动续费，App Store 管理 |

**次卡 ≠ 订阅**，代码中不得将次卡归类为订阅类型。

---

## 一、产品概述

悬浮提词器提供三种付费方式解锁全部功能，覆盖不同用户需求：

| 付费方式 | 产品类型 | 价格 | 适用人群 |
|---------|---------|------|---------|
| 次卡 | 消耗型（Consumable） | ¥1/次 | 偶尔使用的用户 |
| 月卡 | 自动续费订阅（Auto-Renewable） | ¥10/月 | 短期长期使用的用户 |
| 年卡 | 自动续费订阅（Auto-Renewable） | ¥88/年 | 长期高频使用的用户（最优惠） |

免费试用额度为 7 次，用完后显示付费墙，用户必须选择一种方式才能继续使用。

---

## 二、产品定义

### 2.1 次卡

| 属性 | 值 |
|------|-----|
| 产品 ID | `com.teleprompter.per_use_1` |
| StoreKit 类型 | Consumable（消耗型） |
| 价格（国内） | ¥1/次 |
| 有效期 | 无过期时间，用完即止 |
| 续费方式 | 不自动续费，用完需再次购买 |
| 购买数量 | 可买 1~100 次，支持快速选择（1、5、10、20、50次），点击后直接设置数量，不立即调起支付 |

### 2.2 月卡

| 属性 | 值 |
|------|-----|
| 产品 ID | `com.teleprompter.monthly` |
| StoreKit 类型 | Auto-Renewable Subscription（自动续费订阅） |
| 价格（国内） | ¥10/月 |
| 有效期 | 30 天 |
| 续费方式 | 自动续费，App Store 管理 |
| Subscription Group | `group1`（与年卡同一组） |

### 2.3 年卡

| 属性 | 值 |
|------|-----|
| 产品 ID | `com.teleprompter.yearly` |
| StoreKit 类型 | Auto-Renewable Subscription（自动续费订阅） |
| 价格（国内） | ¥88/年 |
| 有效期 | 365 天 |
| 续费方式 | 自动续费，App Store 管理 |
| Subscription Group | `group1`（与月卡同一组） |

---

## 三、付费墙页面 UI 结构

### 3.1 页面对话框

- 弹出方式：`.sheet` 模态弹出
- 导航栏标题："升级会员"
- 关闭按钮：导航栏右侧
- 跟随系统深色/浅色模式

### 3.2 页面内容（从上到下）

```
┌──────────────────────────────┐
│  🏆 皇冠图标                 │
│  "解锁无限使用"               │
│  "免费额度用完后，选择适合的方式继续使用" │
├──────────────────────────────┤
│  当前状态卡片                  │
│  ✅ 图标 + 状态文字            │
│  （显示免费剩余/按次剩余/会员到期信息） │
├──────────────────────────────┤
│  选择适合的方式                 │
│  📊 次卡 - 买固定次数       │
│  ♾️ 会员订阅 - 无限使用+自动续费 │
│  👑 全部功能 - 均解锁所有功能    │
├──────────────────────────────┤
│  选择方案                      │
│                              │
│  [次卡卡片]  ¥?            │
│  （已有订阅时灰色不可点，显示提示） │
│  （选择后展开次数选择器）        │
│                              │
│  [月卡卡片]    ¥?/月           │
│  [年卡卡片]    ¥?/年  🏷最优惠  │
│  （当前方案显示"当前方案"标签，灰色）│
├──────────────────────────────┤
│  [购买按钮]                    │
│  恢复购买                      │
│  隐私政策 ｜ 服务条款            │
└──────────────────────────────┘
```

### 3.3 已有订阅时的绿色提示横幅

在方案选择区顶部，显示：
```
✅ 您已有生效的订阅
到期时间: 2026-06-12
订阅由 App Store 自动管理，到期后自动续费，无需手动操作。
如需切换方案，可直接选择其他订阅。

当前方案: 月卡/年卡
```

---

## 四、交互逻辑

### 4.1 状态判断矩阵

| 用户当前状态 | 次卡 | 月卡 | 年卡 |
|------------|---------|------|------|
| 无订阅、有免费次数 | ✅ 可选 | ✅ 可选 | ✅ 可选 |
| 无订阅、免费用完 | ✅ 可选 | ✅ 可选 | ✅ 可选 |
| 已有月卡 | ❌ 灰色（会员无需按次） | ❌ 灰色 + 标签"当前方案" | ✅ 可选（升级至年卡） |
| 已有年卡 | ❌ 灰色（会员无需按次） | ✅ 可选（降级至月卡） | ❌ 灰色 + 标签"当前方案" |
| 年卡降级月卡待生效 | ❌ 灰色 | ❌ 灰色 | ❌ 灰色 |
| 已有次卡 | ✅ 可选（累加次数） | ✅ 可选（次卡保留，订阅优先） | ✅ 可选（次卡保留，订阅优先） |

### 4.2 跨级切换规则（2026-05-15 更新）

| 切换方向 | 类型 | 生效时机 | 购买后限制 |
|---------|------|---------|-----------|
| 月卡→年卡 | 升级 | 立即生效 | 年卡置灰，月卡可选（可降级） |
| 年卡→月卡 | 降级 | 年卡到期后生效 | **年卡到期前全部置灰**，到期后月卡生效 |

**降级特殊逻辑**：
- 年卡用户购买月卡 = 降级操作
- 月卡不会立即生效，需等待年卡到期
- 年卡到期前，用户**无法购买任何订阅**（包括再次购买年卡或月卡）
- 年卡到期后，系统自动转为月卡，届时月卡开始自动续费

### 4.3 购买按钮文案

| 场景 | 按钮文案 |
|------|---------|
| 未选择任何方案 | "选择方案"（灰色不可点） |
| 选择了次卡（1次） | "购买 1 次 ¥1" |
| 选择了次卡（10次） | "购买 10 次 ¥10" |
| 选择月卡 | "订阅月卡 ¥10/月" |
| 选择年卡 | "订阅年卡 ¥88/年" |
| 月卡→年卡（升级） | "升级至年卡 ¥88/年" |
| 年卡→月卡（降级） | "降级至月卡 ¥10/月" |

### 4.4 购买成功提示（2026-05-15 更新）

| 场景 | 提示文案 |
|------|---------|
| 新购月卡 | "月卡订阅成功！会员权益已生效，到期后自动续费" |
| 新购年卡 | "年卡订阅成功！会员权益已生效，到期后自动续费" |
| 月卡升级年卡 | "年卡升级成功！立即享受年卡权益" |
| 年卡降级月卡 | "月卡订阅成功，将于xx年xx月xx日年卡到期后自动转为月卡" |
| 次卡 | "已获得 X 次使用权限" |

**注意**：相同订阅类型已在UI置灰不可选，不会触发购买，无需提示。

### 4.3 购买成功后

| 场景 | 成功提示 |
|------|---------|
| 次卡 | "已获得 X 次使用权限" |
| 月卡/年卡 | "订阅成功！会员权益已生效，系统将自动续费。" |

次卡购买后不会自动关闭付费墙，用户可以继续购买其他方案。订阅购买后自动关闭付费墙。

**次卡与订阅共存规则**：
- 已有次卡可以购买订阅，次卡次数保留
- 订阅有效期内优先使用订阅（无限次），不扣次卡次数
- 订阅到期或取消后，自动恢复使用次卡（扣次数）
- 次卡次数永久有效，不会因购买订阅而清空

### 4.4 购买失败

- 显示 Alert："购买失败" + 错误原因
- 不清除用户的选择，方便用户重试

---

## 五、Cross-grade 升降级规则

月卡和年卡在同一 Subscription Group（`group1`），由 StoreKit 2 原生处理升降级：

| 切换方向 | 类型 | 生效时机 | 续费价格 | 退款处理 |
|---------|------|---------|---------|---------|
| 月卡→年卡 | 升级（Upgrade） | 立即生效 | 按年卡 ¥88/年 | Apple 按比例退款剩余月卡天数 |
| 年卡→月卡 | 降级（Downgrade） | 当前年卡到期后生效 | 到期后按月卡 ¥10/月 | 无退款，年卡继续用到到期 |

用户不需要手动取消旧的订阅，StoreKit 自动处理切换。

### 本地凭证同步

当发生升降级时，本地保存的订阅凭证类型也要同步更新：

```
跨级前: 本地凭证 type = .monthly
跨级后: 本地凭证 type 更新为 .yearly（同时延长有效期）
```

---

## 六、价格显示规则

### 6.1 核心原则

**所有价格显示必须使用实际 StoreKit 产品价格**，禁止硬编码。

### 6.2 格式化规则

```
产品单价（如 ¥1、¥10、¥88）
  → 用 NumberFormatter(currencyCode: "CNY", locale: zh_CN)
  → 清除 "CN¥" 前缀为 "¥"

多份总价（如 10次 × ¥1）
  → product.price × perUseCount → 同样格式化为 CNY
```

这保证按钮上的显示价格与 StoreKit 购买弹窗的实际扣费价格一致。

### 6.3 App Store Connect 价格配置要求

| 产品 ID | 中国区售价 |
|---------|-----------|
| `com.teleprompter.per_use_1` | ¥1.00 |
| `com.teleprompter.monthly` | ¥10.00 |
| `com.teleprompter.yearly` | ¥88.00 |

---

## 七、数据流

### 7.1 整体架构

```
[用户操作] → PaywallView
                │
    ┌───────────┼───────────┐
    ▼           ▼           ▼
StoreManager  TicketManager  UsageTracker
(StoreKit)    (Keychain)    (UserDefaults)
    │           │
    ▼           ▼
StoreKit API  本地凭证存储
    │
    ▼
Transaction.currentEntitlements
```

### 7.2 三个管理器的职责

| 管理器 | 存储位置 | 职责 |
|--------|---------|------|
| `StoreManager` | StoreKit（苹果服务器） | 加载产品、发起购买、验证交易、查询当前订阅状态 |
| `TicketManager` | Keychain（本地） | 管理本地凭证（次卡次数、订阅有效期、免费次数） |
| `UsageTracker` | UserDefaults | 记录免费试用次数（7次） |

**凭证优先级**：订阅 > 次卡 > 免费次数
- 订阅有效期内：无限次使用，不扣次卡
- 订阅到期后：自动降级使用次卡（扣次数）
- 次卡用完：使用免费次数（如有）
- 全部用完：显示付费墙

### 7.3 权限检查链

```
用户使用功能时：
  UsageTracker.recordUsage()
    → 如果 isLocked（免费次数用完）
      → TicketManager.canUseFeature?
        1. hasSubscription?（本地或 StoreKit 有订阅）
        2. hasPerUse?（有次卡次数）
        3. freeUsesRemaining > 0?（还有免费次数）
```

### 7.4 购买成功后的凭证创建

```
StoreKit 返回 .success
  → StoreManager.checkPurchasedProducts()  // 刷新订阅状态
  → PaywallView.handlePurchaseState()
    → 次卡: TicketManager.addPerUseTickets(count: X)
    → 月卡/年卡: TicketManager.extendSubscription(type: X, duration: 1)
```

---

## 八、App Store Connect 配置要求

### 8.1 产品列表

| 参考名 | 产品 ID | 类型 | 定价 |
|-------|--------|------|------|
| 次卡 | `com.teleprompter.per_use_1` | Consumable | 中国区 ¥1 |
| 月卡 | `com.teleprompter.monthly` | Auto-Renewable Subscription | 中国区 ¥10/月 |
| 年卡 | `com.teleprompter.yearly` | Auto-Renewable Subscription | 中国区 ¥88/年 |

### 8.2 订阅组

- 月卡和年卡必须放在同一个 Subscription Group 内（如 `group1`）
- Subscription Group 名称自定义即可

### 8.3 本地测试配置

StoreKit 配置文件 `Teleprompter.storekit` 需包含三个产品，与 App Store Connect 的产品 ID 完全一致。

---

## 九、本地 StoreKit 配置 (Teleprompter.storekit)

```xml
<dict>
    <key>identifier</key>
    <string>com.teleprompter.storekit</string>
    <key>products</key>
    <array>
        <!-- 次卡 -->
        <dict>
            <key>identifier</key>
            <string>com.teleprompter.per_use_1</string>
            <key>type</key>
            <string>Consumable</string>
            <key>displayName</key>
            <string>单次使用</string>
            <key>description</key>
            <string>单次使用权限</string>
            <key>price</key>
            <real>1.0</real>
            <key>locale</key>
            <string>zh_CN</string>
        </dict>
        <!-- 月卡 -->
        <dict>
            <key>identifier</key>
            <string>com.teleprompter.monthly</string>
            <key>type</key>
            <string>Auto-Renewable Subscription</string>
            <key>displayName</key>
            <string>月度会员</string>
            <key>description</key>
            <string>按月订阅会员</string>
            <key>price</key>
            <real>10.0</real>
            <key>locale</key>
            <string>zh_CN</string>
            <key>subscriptionPeriod</key>
            <string>P1M</string>
            <key>subscriptionGroupID</key>
            <string>group1</string>
        </dict>
        <!-- 年卡 -->
        <dict>
            <key>identifier</key>
            <string>com.teleprompter.yearly</string>
            <key>type</key>
            <string>Auto-Renewable Subscription</string>
            <key>displayName</key>
            <string>年度会员</string>
            <key>description</key>
            <string>按年订阅会员</string>
            <key>price</key>
            <real>88.0</real>
            <key>locale</key>
            <string>zh_CN</string>
            <key>subscriptionPeriod</key>
            <string>P1Y</string>
            <key>subscriptionGroupID</key>
            <string>group1</string>
        </dict>
    </array>
</dict>
```

---

## 十、核心代码文件索引

| 文件 | 用途 | 关键类/结构 |
|------|------|------------|
| `Store/StoreManager.swift` | StoreKit 内购管理器 | `StoreManager`、`ProductID`、`SubscriptionType`、`PurchaseState` |
| `Store/TicketManager.swift` | 本地凭证管理器 | `TicketManager`、`ServiceTicket`、`TicketType`、`KeychainWrapper` |
| `Store/PaywallView.swift` | 付费墙 UI | `PaywallView`、`PerUseCard`、`SubscriptionCard` |
| `Store/MembershipView.swift` | 会员中心 UI | `MembershipView`、`BenefitRow`、`FAQItem` |
| `Store/PaymentTestView.swift` | 支付测试页面 | `PaymentTestView`（DEBUG 模式） |
| `Teleprompter.storekit` | StoreKit 本地测试配置 | 三个产品的定价和订阅组 |
| `Features/Usage/UsageTracker.swift` | 免费试用次数管理 | `UsageTracker` |
| `Supporting/Constants.swift` | 常量定义 | `maxFreeTrials = 7` |

---

## 十一、已确认不可修改的功能

1. 次卡产品（per_use）保留，消耗型，用完即止
2. 月卡/年卡为自动续费订阅，同一 Subscription Group
3. 已有生效订阅时不可购买次卡
4. 不可重复购买相同产品
5. 月卡⇄年卡可跨级切换，StoreKit 原生处理
6. 已有订阅时显示绿色"自动续费"提示横幅
7. 价格显示必须使用实际 product.price，禁止硬编码
8. 升降级时本地凭证 type 同步更新

---

## 十二、购买流程核心逻辑（2026-05-13 修正确认）

### 12.1 购买成功后的订阅类型确定

**规则**：购买成功后，直接从刚完成的 `transaction.productID` 确定用户的订阅类型，**不依赖** `Transaction.currentEntitlements` 的查询结果。

**原因**：`Transaction.currentEntitlements` 在沙盒/TestFlight 环境中有刷新延迟。用户取消旧订阅后购买新订阅，entitlements 可能同时包含新旧订阅（旧订阅当期仍有效），导致订阅类型识别错误。

**正确流程**：

```
用户完成支付 → StoreKit 返回 .success(.verified(transaction))
  → 检查 transaction.productID
    → "com.teleprompter.yearly"  → activeSubscriptionType = .yearly
    → "com.teleprompter.monthly" → activeSubscriptionType = .monthly
  → isPremium = true（刚购买成功一定是会员）
  → transaction.finish()
  → checkPurchasedProducts()（仅用于更新到期日期，不用于判断类型）
  → purchaseState = .success
```

**错误流程（原始代码）**：

```
用户完成支付 → StoreKit 返回 .success
  → transaction.finish()
  → checkPurchasedProducts() 去 entitlements 查 ← ❌ entitlements 可能还没更新
  → 查到旧订阅（如已取消但当期仍有效的月卡）→ activeSubscriptionType = .monthly ← ❌ 错误
```

### 12.2 重复购买检查

**规则**：禁止重复购买同一产品，检查必须同时覆盖 StoreKit 订阅状态和本地凭证状态。

**检查条件**（任一满足即拦截）：

```swift
// StoreKit 层面：苹果服务器认定的当前订阅
if storeManager.activeSubscriptionType == subscription {
    // 例：activeSubscriptionType = .yearly, subscription = .yearly → 拦截
}

// 本地凭证层面：本地 Keychain 中已有的有效凭证
if let localTicket = ticketManager.subscriptionTicket,
   localTicket.isValid,
   localTicket.type.rawValue == subscription.rawValue {
    // 例：本地有有效年卡凭证，用户又想买年卡 → 拦截
}
```

**原因**：仅依赖 StoreKit 的 `activeSubscriptionType` 不够。
- StoreKit 的 `Transaction.currentEntitlements` 可能因刷新延迟未包含新购买的订阅
- 本地 TicketManager 的凭证独立于 StoreKit 存储，可以即时判断
- 两者同时检查可以覆盖"StoreKit 还没刷到"的空窗期

**跨级场景不受影响**：
- 月卡生效 + 点年卡 → `activeSubscriptionType(.monthly) != .yearly` + 本地 type(.monthly) != .yearly → **放行** ✅
- 年卡生效 + 点月卡 → `activeSubscriptionType(.yearly) != .monthly` + 本地 type(.yearly) != .monthly → **放行** ✅

### 12.3 本地凭证延长条件

**规则**：必须在确认"是真的新购买了"之后才执行 `extendSubscription()`，不能无条件信任 StoreKit 的 `.success`。

**判断依据**：
- StoreKit 购买弹窗弹过并完成支付（不是"已订阅此项目"的自动完成）
- 或者 `checkPurchasedProducts()` 确认 entitlements 中出现了新订阅

**安全措施**：在 `handlePurchaseState(.success)` 中处理本地凭证前，再次检查当前是否已有同类型有效凭证。如果有则不延长。

---

## 十三、已知 Bug 记录

### Bug #1：年卡可无限重复购买（无需支付）

**状态**：待修复（代码已定位）

**场景**：
1. 用户月卡生效
2. 点击"升级至年卡" → StoreKit 弹出"你已订阅此项目"（年卡之前测试买过）
3. 点"好" → 不弹支付页 → StoreKit 返回 `.success`
4. `checkPurchasedProducts()` 没发现年卡 → `activeSubscriptionType` 保持 `.monthly`
5. `extendSubscription(type: .yearly, duration: 1)` 无条件给本地年卡延长 1 年
6. 回到步骤 2，无限循环

**根因（三条叠加）**：

| 问题 | 文件:行号 | 根因 |
|------|----------|------|
| A | StoreManager.swift:227-233 | `handlePurchaseResult` 依赖 `checkPurchasedProducts()` 查 entitlements 确定订阅类型，但 entitlements 刷新有延迟 |
| B | PaywallView.swift:321 | 重复购买检查只对比 StoreKit 的 `activeSubscriptionType`，没检查本地 TicketManager 凭证 |
| C | PaywallView.swift:476 | `extendSubscription()` 无条件执行，没校验"是否真的是新购买" |

**修复方向**：
1. **StoreManager.swift**：`handlePurchaseResult` 中直接从 `transaction.productID` 确定类型
2. **PaywallView.swift**：重复购买检查增加本地凭证对比
3. **PaywallView.swift**：`extendSubscription()` 前校验是否有同类型有效凭证
