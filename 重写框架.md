# 提词器会员购买页 - 代码框架

## 一、产品规则

| 规则 | 说明 |
|------|------|
| 月卡/年卡 | 都是自动续费订阅，同一 Subscription Group |
| 不支持手动续费 | 月卡有月卡时不能再买月卡，年卡有年卡不能再买年卡 |
| 跨级切换 | 月卡↔年卡走叠加逻辑（StoreKit 原生处理升降级） |
| 次卡 | 有月卡或年卡时不可购买次卡 |
| 免费试用 | 7次，统一改 |
| 价格显示 | 动态获取 product.price，自适应货币 |

## 二、状态判断矩阵

| 用户状态 | 次卡 | 月卡 | 年卡 |
|---------|---------|------|------|
| 无订阅 | ✅ 可选 | ✅ 可选 | ✅ 可选 |
| 已有月卡 | ❌ 灰色 | ❌ 灰色"当前方案" | ✅ 可选"升级至年卡" |
| 已有年卡 | ❌ 灰色 | ✅ 可选"切换至月卡" | ❌ 灰色"当前方案" |

## 三、UI 结构

```
┌──────────────────────────────────┐
│           关闭按钮                │
│                                  │
│        🏆 皇冠图标               │
│      "解锁无限使用"               │
│    "免费额度用完后，选择方案"       │
├──────────────────────────────────┤
│  当前状态卡片（有订阅时显示绿色）    │
│  ✅ 您已有生效的订阅              │
│  到期时间: 2026-06-12            │
│  App Store 自动管理              │
│  当前方案: 月卡/年卡              │
├──────────────────────────────────┤
│  方案选择                        │
│                                  │
│  [次卡卡片]  ¥?               │
│  （有订阅时灰色不可点）            │
│                                  │
│  [月卡卡片]    ¥?/月              │
│  （有月卡时灰色"当前方案"）        │
│                                  │
│  [年卡卡片]    ¥?/年  🏷最优惠    │
│  （有年卡时灰色"当前方案"）        │
├──────────────────────────────────┤
│  [购买按钮]                      │
│  恢复购买 | 隐私政策 | 服务条款    │
└──────────────────────────────────┘
```

## 四、购买按钮文案

| 场景 | 文案 |
|------|------|
| 未选择 | "选择方案"（灰色不可点） |
| 次卡 | "购买 X 次 ¥X" |
| 月卡 | "订阅月卡 ¥10/月" |
| 年卡 | "订阅年卡 ¥88/年" |
| 月卡→年卡 | "升级至年卡 ¥88/年" |
| 年卡→月卡 | "切换至月卡 ¥10/月" |

## 五、核心代码逻辑

### 1. StoreManager.swift

```swift
// 购买函数 - 不提前拦截，直接调用 StoreKit
func purchase(_ product: Product) async {
    purchaseState = .purchasing
    let result = try await product.purchase()
    await handlePurchaseResult(result)
}

// 处理购买结果 - 直接从 transaction.productID 确定类型
func handlePurchaseResult(_ result: Product.PurchaseResult) async {
    case .success(let verification):
        if case .verified(let transaction) = verification {
            // 直接从 transaction 取 productID 确定购买的订阅类型
            pendingPurchaseProductID = transaction.productID
            await transaction.finish()
            await checkPurchasedProducts()
            purchaseState = .success
        }
}

// 新增属性：记录刚完成购买的产品ID
var pendingPurchaseProductID: String?
```

### 2. PaywallView.swift - 购买按钮点击

```swift
func handlePurchaseButton() {
    if selectedSubscription == .monthly && storeManager.activeSubscriptionType == .monthly {
        return  // 已有月卡，不允许重复购买
    }
    if selectedSubscription == .yearly && storeManager.activeSubscriptionType == .yearly {
        return  // 已有年卡，不允许重复购买
    }
    if showPerUsePicker && storeManager.isPremium {
        return  // 有订阅，不允许次卡
    }
    // 执行购买
    await storeManager.purchase(product)
}
```

### 3. PaywallView.swift - 购买成功后

```swift
func handlePurchaseState(_ state: PurchaseState) {
    case .success:
        // 1. 先检查是否是真正的重复购买
        guard !isDuplicatePurchase() else {
            storeManager.purchaseState = .idle
            return
        }
        
        // 2. 根据实际购买的产品ID创建凭证
        if let productID = storeManager.pendingPurchaseProductID {
            switch productID {
            case "com.teleprompter.per_use_1":
                ticketManager.addPerUseTickets(count: perUseCount)
            case "com.teleprompter.monthly":
                ticketManager.extendSubscription(type: .monthly, months: 1)
            case "com.teleprompter.yearly":
                ticketManager.extendSubscription(type: .yearly, months: 1)
            }
        }
        
        showSuccess = true
        storeManager.purchaseState = .idle
}

// 重复购买检查（双重检查 StoreKit + 本地凭证）
func isDuplicatePurchase() -> Bool {
    // StoreKit 层面
    if storeManager.activeSubscriptionType == selectedSubscription {
        return true
    }
    // 本地凭证层面
    if let ticket = ticketManager.subscriptionTicket,
       ticket.isValid,
       ticket.type == selectedSubscription {
        return true
    }
    return false
}
```

### 4. 免费试用次数统一为7

Constants.swift 中 maxFreeTrials = 7

## 六、文件修改清单

| 文件 | 修改内容 |
|------|---------|
| StoreManager.swift | 添加 pendingPurchaseProductID，handlePurchaseResult 从 transaction.productID 确定类型 |
| PaywallView.swift | 重写UI，重复购买检查，extendSubscription 前校验 |
| Constants.swift | maxFreeTrials 改为 7 |
| TicketManager.swift | 不改核心逻辑 |

## 七、待确认

1. 框架是否 OK？
2. 价格动态获取用 product.displayPrice 还是 NumberFormatter？
3. 需要我列出需要删除的旧代码吗？