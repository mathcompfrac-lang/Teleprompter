# 悬浮提词器 - 设计文档

## 项目概述
iOS 悬浮提词器 App，利用 AVPictureInPicture 在其他 App 上方浮动显示提词文本。

## 技术栈
- **语言**: Swift 5 (Swift 6 兼容模式)
- **UI 框架**: SwiftUI + UIKit (PIP 部分)
- **最低 iOS**: 17.0
- **持久化**: SwiftData
- **音频**: AVFoundation (AVAudioEngine)
- **画中画**: AVKit (AVPictureInPicture)

## 已确认成功功能（不得擅自修改）

### 1. 项目骨架与编译
- Xcode 项目通过 generate_xcodeproj.py 自动生成
- 零第三方依赖，全部系统内置
- 编译已验证通过

### 2. 用户界面
- 主界面：顶部状态栏 + 中间提词预览 + 底部控制面板
- 脚本管理：列表 + 编辑器 + 搜索
- 设置页面：字号、语速、窗口尺寸、语音倍率
- 7 次试用计数

### 3. 签名配置
- Team ID: 2P5UG67845
- Bundle ID: com.teleprompter.1777642669
- 自动签名已配置

### 4. 进度条同步主视图滚动 ✅ 用户确认生效 (2026-05-01)
- 改用 UIScrollView UIViewRepresentable 包装器直接操作 contentOffset
- 添加 `layoutIfNeeded()` 确保 contentSize 正确
- **语音模式和手动模式下均确认有效** 🛑
- 根因修复：viewModel 从 `@State` 改为 `@ObservedObject`，ContentView 开始监听 `@Published` 变化

### 5. 画中画不可用提示 ✅ 用户确认生效 (2026-05-01)
- 模拟器上点击画中画按钮显示警告弹窗
- 提示用户需要在真机上使用
- **已确认功能，代码不做更改** 🛑

### 6. 语音降级定时滚动 ✅ 用户确认生效 (2026-05-01)
- 降级逻辑移到 TeleprompterViewModel，独立于 SpeechDetector
- 语音模式下点击播放即启动 fallbackTimer
- 新增控件：Toggle "无麦克风时自动滚动" + Stepper 1~20字/秒
- 默认开启，5字/秒
- **已确认功能，代码不做更改** 🛑

### 7. 手动模式不自动滚动 ✅ 用户确认生效 (2026-05-01)
- 手动模式下点播放不再启动定时器，仅标记活跃
- 恢复滚动时也不启动定时器
- 用户自行拖进度条或滚动预览控制
- **已确认功能，代码不做更改** 🛑

### 8. 脚本编辑功能 ✅ 用户确认生效 (2026-05-02)
- 脚本列表左滑→编辑，可编辑已有脚本标题和内容
- 改用 `.sheet(item:)` 确保传入正确脚本对象
- **已确认功能，代码不做更改** 🛑

### 9. 主题配置 ✅ 用户确认生效 (2026-05-02)
- 设置页提供浅色/深色/跟随系统三选项
- 通过 `window.overrideUserInterfaceStyle` 窗口级别强制生效
- **已确认功能，代码不做更改** 🛑

## 悬浮窗功能（方案A：AVPictureInPicture）

### 技术限制
- PIP 内不支持触摸交互（按钮、滑块、手势）
- 控制按钮全部放在主 APP 界面
- PIP 只负责渲染：文本高亮、透明度、字体颜色、字号

### 已实现功能 ✅ (2026-05-02)

#### 0. 基础功能
- 脚本创建/编辑/选择（左滑编辑已有脚本）🛑
- 进度条同步主视图滚动（语音+手动双模式）🛑
- 手动模式不自动滚动（播放不启动定时器）🛑
- 语音降级定时滚动（开关 + 速度配置 1~20字/秒）🛑

#### 1. 已读文本高亮
- 语音/手动模式下，已读部分用高亮颜色显示
- 使用 `NSAttributedString` 局部变色
- 主 APP 预览区和 PIP 悬浮窗同步高亮
- 设置页可选 8 种高亮颜色
- **已确认功能，代码不做更改** 🛑

#### 2. 背景透明度
- PIP 悬浮窗背景透明度可调（0.3~1.0）
- 设置页新增透明度滑块
- **已确认功能，代码不做更改** 🛑

#### 3. 文本颜色
- 主 APP 预览区和 PIP 文本颜色可调
- 设置页新增颜色选择器（10 种颜色，横向滚动）
- **已确认功能，代码不做更改** 🛑

#### 4. 颜色色板可滚动
- 颜色超出屏幕宽度时自动可横向滑动
- **已确认功能，代码不做更改** 🛑

#### 5. 主题配置
- 浅色/深色/跟随系统（窗口级别强制生效）
- **已确认功能，代码不做更改** 🛑

#### 6. 按钮放大（真机适配）
- 顶部栏、控制面板所有按钮图标放大到 25~30pt
- **已确认功能，代码不做更改** 🛑

### 10. 悬浮窗启动修复 ✅ 用户确认生效 (2026-05-02)
- PIP 启动前设置 AudioSession 为 .playback（必须）
- 创建 AVPictureInPictureController 后延迟 0.5 秒调用 startPictureInPicture()
- **已确认功能，代码不做更改** 🛑

### 11. 设置回调链路修复 ✅ 2026-05-02
- SettingsView 缺少 init，onSettingsChanged 闭包从未赋值给属性
- 添加 init(onSettingsChanged:) 确保设置变更同步到 PIPManager
- **已确认功能，代码不做更改** 🛑

### 12. 主 app 文字颜色固定 ✅ 2026-05-02
- 主 app 预览区固定：黑色文字 + 黄色高亮（#FFD700）
- 设置页颜色选择器仅影响悬浮窗（标签改为"悬浮窗外观"）
- **已确认功能，代码不做更改** 🛑

### 13. fallbackScrollEnabled 默认关闭 ✅ 2026-05-02
- 无麦克风时自动滚动开关默认改为 false

### 14. 自动续费订阅功能 ✅ 用户确认生效 (2026-05-12)
- 月卡（com.teleprompter.monthly）：自动续费订阅，¥10/月
- 年卡（com.teleprompter.yearly）：自动续费订阅，¥88/年
- 月卡和年卡在同一 Subscription Group（group1），支持 StoreKit 自动跨级/升降级
- **已确认功能，代码不做更改** 🛑

### 15. 订阅交互逻辑 ✅ 用户确认生效 (2026-05-12)
- 已有生效订阅时：显示"您已有生效的订阅，系统自动续费"提示横幅
- 不可重复购买相同产品（已订阅月卡不能再次购买月卡）
- 可跨级切换（月卡⇄年卡），StoreKit 自动处理升降级
- 月卡和年卡购买提示保持一致，统一文案

**Cross-grade 规则（Apple StoreKit 原生机制）**：
| 切换方向 | 生效时机 | 续费价格 | 退款处理 |
|---------|---------|---------|---------|
| 月卡→年卡（升级） | 立即生效 | 按年卡 ¥88/年 | Apple 按比例退款剩余月卡 |
| 年卡→月卡（降级） | 当前年卡到期后生效 | 到期后按月卡 ¥10/月 | 无退款，年卡继续用到到期 |

- 切换后不需要用户手动取消旧的订阅，StoreKit 自动处理
- **已确认功能，代码不做更改** 🛑

### 16. 按次付费保留 ✅ 用户确认生效 (2026-05-12)
- 保留 com.teleprompter.per_use_1 消耗型产品
- 不受已有订阅影响的独立的购买选项
- 但已有生效订阅时，按次付费不可点击（会员期间无需按次购买）
- **已确认功能，代码不做更改** 🛑

### 17. 悬浮提词全部功能确认 ✅ 用户确认生效 (2026-05-12)
- 悬浮窗启动、文本同步、透明度设置等全部悬浮提词相关功能已确认
- **已确认功能，代码不做更改** 🛑

### 18. 购买流程核心逻辑确认 ✅ (2026-05-13)
- 购买成功后从 `transaction.productID` 直接确定订阅类型，不依赖 entitlements 查询
- 重复购买检查同时覆盖 StoreKit `activeSubscriptionType` 和本地 TicketManager 凭证
- `extendSubscription` 需确认是新购买后才执行
- **详见 `付费功能设计.md` 第十二、十三章**

### 语音追踪（未解决）
- SFSpeechRecognizer 中文识别每 7~9 字后 isFinal 中断
- 尝试过：前缀匹配、增量匹配、双缓冲识别、本地/服务器切换——均无效
- 待换引擎：WhisperKit（免费/本地/SPM集成）或讯飞

## 数据流

```
[用户输入] → TeleprompterViewModel
                  │
        ┌─────────┼─────────┐
        ▼         ▼         ▼
   SpeechDetector Timer   PIPManager
   (语音驱动)    (手动)  (画中画渲染)
        │         │         │
        └─────────┴─────────┘
                  │
              TextOffset / scrollProgress
                  │
        ┌─────────┴─────────┐
        ▼                   ▼
   主视图 ScrollView    PIPContentViewController
   (行标记 + ScrollTo)  (UIScrollView + UILabel)
```

## Bug 记录

### Bug #1: 语音模式字幕不滚动 ✅ 已修复
- **根因**: SpeechDetector 在模拟器无音频输入，从未调用推进回调
- **修复**: 添加降级定时器，无输入 3 秒后自动推进

### Bug #2: 手动模式进度条不控制字幕 ✅ 已修复
- **根因**: seekTo 更新 scrollProgress 但只同步到 PIP 视图，主视图未跟随
- **修复**: 主视图改用 ForEach 每行渲染 + ScrollViewReader + scrollTo

### Bug #3: 画中画按钮无效 ✅ 已修复
- **根因**: 模拟器不支持 AVPictureInPicture，startPIP 返回 false 但无用户反馈
- **修复**: 点击时检查 isPIPAvailable，不可用时弹 Alert 提示

